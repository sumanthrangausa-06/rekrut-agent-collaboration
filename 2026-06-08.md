# Memory Update — 2026-06-08

## Stripe Webhook Secrets — DONE ✅
- Added `whsec_fOoWveovu6BoWb8KiWUTB9R4yLOb02v0` to dev Render env vars
- Added `whsec_1t6MIj8F0j2RvFelyXr130Zli6VSUWrN` to prod Render env vars
- Added all Stripe keys (publishable, secret, webhook) to prod
- Tested dev checkout session: WORKING ✅ (returns valid Stripe URL)
- Tested dev webhook endpoint: WORKING ✅ (rejects invalid signatures)

## Cron Jobs — FIXED ✅
- Disabled failing 2-minute heartbeat (timeout errors)
- Changed work batch from 2 hours → 30 minutes
- Updated work batch to rotate focus areas (not just app dev)

## Prod Deploy — IN PROGRESS 🔄
- Merged dev → main (clean merge, no conflicts)
- Pushed to GitHub
- Render auto-deploy triggered: `dep-d8j0trk2m8qs73996f7g`
- Status: `build_in_progress`
- Monitoring for completion

## Image Audit — DOCUMENTED ✅
- Created `docs/IMAGE_AUDIT.md`
- App uses only Lucide icons and text — no photos/illustrations
- Identified all pages needing image assets
- Created quick-win plan (Unsplash, DiceBear, Undraw)
- Waiting for Ranga's style direction (photos vs illustrations vs mixed)

## Workflow Doc — SHARED ✅
- Created `docs/SUGA_WORKFLOW.md` for Kimiclaw
- Documents: cron setup, delegation rules, git workflow, Render API, agent templates
- No more group chat posts — direct communication only

## Prod Deploy Status — IN PROGRESS 🔄
- Commit e67505b pushed to main
- Client vulnerabilities fixed (vite/rollup path traversal)
- Build passes, E2E navigation tests pass
- Still needs Ranga approval on: Stripe live keys, DNS/domain, plan upgrade, monitoring setup

## Work Batch (12:52 SGT) — DONE ✅
- npm audit fix client: 7 vulns → 0 (vite/rollup dev deps)
- Build verified
- E2E navigation-flow: 4/4 passed
- Committed uncommitted e2e text-match fixes + dist rebuild
- Pushed to main

## Work Batch (22:52 SGT) — COMPLETE ✅
- **frontend-developer**: Mobile responsive job detail panel fix (95% → 100% target) — DONE in commit 88e53f6
- **model-qa-specialist**: E2E test suite expansion, individual file execution (~55% → 70% target) — DONE: recruiter-applicant-review-flow.spec.ts committed as c42fcc8
- **devops-automator**: Prod deployment readiness assessment and documentation — DONE: PROD_DEPLOY_CHECKLIST.md and RUNBOOK updated

## Heartbeat (05:56 SGT) — Status Check
- Build: passes ✅
- Working tree: clean (committed e2e test + image asset)
- Main branch: ahead of origin by 1 commit (c42fcc8)
- Next: push to origin when work batch resumes

## Next Actions Needed
- Ranga to review PROD_DEPLOY_CHECKLIST.md and approve items in section 6
- E2E: individual files pass, full suite still blocked by browser memory (known issue)
- EU AI Act: 50%, needs legal review

## Prod Service Status
- URL: https://rekrut-ai.onrender.com
- Current deploy: old commit (May 2) — updating now
- New deploy: commit f92f3a9 (dev merge) — building
- Env vars: All Stripe keys added ✅
- Admin password on prod: "changeme123" — NEEDS UPDATE (security)
Staging service is now LIVE and responding at https://rekrutai-staging.onrender.com. Health check returns 200 OK. Root cause was missing environment variables (SESSION_SECRET, JWT_SECRET, and 10+ others). Devops-automator fixed by copying env vars from dev service and adding missing ones. Build succeeds but deploy fails when required env vars missing at module load time. render.yaml has generateValue: true for JWT_SECRET and SESSION_SECRET but these were not applied when service was created manually via API. Render API limitations: PATCH plan update returns 500 internal error, deploy logs endpoint returns 404 for failed deploys. Workaround: delete+recreate service for plan changes, clone repo locally to test startup. Full pipeline now operational: dev auto-deploys to rekrutai-dev, staging auto-deploys to rekrutai-staging, prod requires manual gated deploy. Git branches pushed: dev and staging both at e5be6f6 on origin. Security: one GitHub token ghp_gT...VEPU still needs manual revocation by user at https://github.com/settings/tokens. User directive: Suga is CEO, makes decisions and executes without asking permission. User expects all work delegated to specialized agents, not generic subagents. User prefers direct 1:1 interaction over broadcast group chat delegation.
